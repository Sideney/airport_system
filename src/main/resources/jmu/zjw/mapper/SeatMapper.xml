<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jmu.zjw.mapper.SeatMapper">
    <insert id="insertSeat" parameterType="jmu.zjw.po.Seat">
        insert into Seat(sNum, status, sLevel, ofId) values (#{sNum}, #{status}, #{sLevel}, #{ofId})
    </insert>
    <select id="findSeatById" resultType="jmu.zjw.po.Seat">
        select * from Seat where ofId = #{ofId} and sLevel = #{sLevel}
    </select>
</mapper>