<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jmu.zjw.mapper.AdminMapper">
    <select id="loginAdmin" parameterType="String" resultType="String">
        select aName from Admin where aId = #{aId} and aPassword = #{aPassword}
    </select>
</mapper>